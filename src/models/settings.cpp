struct MagicSettings
{
    int instructionDelay = 0;
    bool useDoubleInstruction = true; // Two instruction in one move (if possible)
    bool isLogging = true; // Turn of serial prints between actions
};